export type SocketUserInfo = {
    avatar: string,
    username: string,
    socketid: string,
    userid: string,
}

export type PrivateMessage = {
    content: string,
    from: string,
    senderid: string,
    sendername: string,
    senderavatar: string
}