/**
 *  This class is used to save the user's chat snapshot, 
 *  when app launch it'll return the last n message with user
 *  info
 * 
 */

import { MessageContent } from "../Interface/user";
export class SnapShot {
    static async storeSnapshot(messages: Array<{

    }>) {

    }
}